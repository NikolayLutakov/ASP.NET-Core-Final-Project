@model AdminGlassesListingViewModel
@{
    ViewData["Title"] = "Glasses Index";
}

<div class="mb-3">
    <a class="btn btn-success" asp-area="Administrator" asp-controller="Glasses" asp-action="Add">Add Glasses</a>
    <a class="btn btn-light" asp-area="Administrator" asp-controller="Home" asp-action="Index">Back</a>
</div>

<partial name="_GlassesListingPartial" model="@Model.Glasses" />

@{
    var previousPage = Model.CurrentPage - 1;
    if (previousPage < 1)
    {
        previousPage = 1;
    }

    var maxPage = Math.Ceiling((double)Model.TotalGlasses / AdminGlassesListingViewModel.GlassesPerPage);
}

@{
    var shouldButtonBeDisabled = Model.CurrentPage == maxPage ||
                                 !Model.Glasses.Any();
}


<div class="paging">
    <nav aria-label="Page navigation example">
        <ul class="pagination">
            <li class="page-item">
                <a class="page-link btn @(Model.CurrentPage == 1 ? "disabled" : string.Empty)"
                   asp-controller="Shop"
                   asp-action="Index"
                   asp-route-currentPage="@previousPage"
                   asp-route-searchTerm="@Model.SearchTerm">Previous</a>
            </li>
            <li class="page-item"><a class="page-link btn disabled" href="#">@Model.CurrentPage of @maxPage</a></li>
            <li class="page-item">
                <a class="page-link btn @(shouldButtonBeDisabled ? "disabled" : string.Empty)"
                   asp-controller="Shop"
                   asp-action="Index"
                   asp-route-currentPage="@(Model.CurrentPage + 1)"
                   asp-route-searchTerm="@Model.SearchTerm">Next</a>
            </li>
        </ul>
    </nav>
</div>